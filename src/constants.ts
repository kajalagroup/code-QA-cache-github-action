export enum Inputs {
    Key = "key",
    Path = "path",
    RestoreKeys = "restore-keys",
    UploadChunkSize = "upload-chunk-size",
    RepoToken = "repo-token"
}


export enum State {
exactMatch = "EXACT_MATCH",
}
  